import React from 'react';

const Header = (props) => {
    return (
        <header className="header">
            <div className="container">
                logo
                <ul>
                    menu
                </ul>
            </div>
        </header>
    );
};


export default Header;
